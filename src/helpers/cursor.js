// let mouseElement = document.querySelector('.mouse');
// let sizes = mouseElement.getBoundingClientRect();
// document.onmousemove = e => {
//     let x = e.pageX;
//     let y = e.pageY;
//     mouseElement.style.top = `${y - sizes.height / 2}px`;
//     mouseElement.style.left = `${x - sizes.width / 2}px`;
// };
